export const environment = {
  production: true,
  // Set this to your backend API URL after deploying the Node.js server
  // (e.g. Cloud Run, Railway, Render, or your own host)
  apiUrl: 'https://your-api-url.com'
};
